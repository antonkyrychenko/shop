import { makeStyles, Theme } from "@material-ui/core/styles";

const useCheckoutPageStyles = makeStyles((theme: Theme) => ({
    root: {
        
    },
    drawerPaper: {
        width: "400px",
        padding: "10px 20px"
    },
}));

export default useCheckoutPageStyles;